package com.myspace.eotworkflow;

import com.myspace.eotworkflow.*;

rule "Auto Doc Gen 1"
    ruleflow-group "autodocgensample"
    when
        document: Document(docname == "OffertoPurchase" && relationshipname == "Buyout" && desc == "EOT")
    then
        Document(autogen == "TRUE");
end

